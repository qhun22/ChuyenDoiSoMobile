export default async function EditProductPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="store-shell admin-shell"><section className="home-section"><p className="eyebrow">Admin / Products / {id}</p><h1>Chinh sua san pham</h1><form className="checkout-form"><label>Ten san pham<input defaultValue="Nova X Pro" required /></label><label>Gia<input type="number" defaultValue={18990000} required /></label><button className="primary-button" type="submit">Cap nhat</button></form></section></main>; }
